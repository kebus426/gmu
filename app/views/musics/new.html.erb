<% provide(:title, "New Music") %>
<% if logged_in? %>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <h1>アップロード</h1>
      <section class="music_form">
        <%= render 'shared/music_form' %>
      </section>
    </div>
  </div>
  <% else %>
  
<% end %>
